# Anthropic API key (required) — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_api_key_here

# Google Gemini API key (optional — only needed for --provider gemini)
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Google service account key path (optional — defaults to ./service-account.json)
# GOOGLE_SERVICE_ACCOUNT_KEY=/path/to/your/service-account.json
